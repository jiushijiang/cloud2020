server:
  port: 9001
spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8847 #nacos服务地址
#开启微服务监控模块
management:
  endpoints:
    web:
      exposure:
        include: '*'